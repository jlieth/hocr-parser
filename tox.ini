[tox]
envlist = py{36,37}

[testenv]
passenv = TOXENV CI TRAVIS TRAVIS_* CODECOV_*
setenv =
    PYTHONPATH = {toxinidir}
    PYTHONWARNINGS = all
deps =
    -rrequirements.txt
    -rrequirements_dev.txt
commands = pytest -v -W all --random-order --doctest-modules hocr_parser {posargs:tests}

[testenv:flake8]
commands = flake8 hocr_parser tests setup.py

[testenv:black]
commands = black --check --diff hocr_parser tests

[testenv:style]
commands =
    {[testenv:flake8]commands}
    {[testenv:black]commands}

[testenv:cover]
commands = pytest --cov=hocr_parser --cov-report=term --cov-report=html --doctest-modules hocr_parser tests

[testenv:coveralls]
deps =
    {[testenv]deps}
    coveralls
commands =
    {[testenv:cover]commands}
    coveralls

[testenv:codecov]
deps =
    {[testenv]deps}
    codecov
commands =
    {[testenv:cover]commands}
    codecov